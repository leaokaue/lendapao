
    
export const randiRange = (min, max) => {
   return Math.round(Math.random() * (max - min) + min);
}



export const randfRange = (min,max) => {
   return  Math.random() * (max - min) + min;
 }
